# AI-EVA Launcher

基于 Tauri 的 AI-EVA 启动器，自动下载和管理 AI-EVA 项目。

## 功能特性

- 🚀 **自动下载**: 从 GitHub 自动下载 AI-EVA 项目文件
- 🎵 **IndexTTS2 集成**: 自动克隆 IndexTTS2 仓库
- 💻 **跨平台**: 支持 Windows、macOS、Linux
- 🎨 **现代化 UI**: 美观的用户界面
- 📊 **进度显示**: 实时显示下载进度

## 开发环境要求

- Node.js 18+
- Rust 1.70+
- Git（用于克隆 IndexTTS2）

## 安装依赖

```bash
# 安装 Node.js 依赖
npm install

# 安装 Rust 依赖（会自动执行）
cargo build
```

## 开发模式

```bash
# 启动开发服务器
npm run tauri dev
```

## 构建应用

```bash
# 构建生产版本
npm run tauri build
```

构建产物位于 `src-tauri/target/release/` 目录。

## 使用说明

1. **配置 GitHub 仓库**: 在界面中输入你的 AI-EVA GitHub 仓库地址
2. **选择安装路径**: 选择或输入 AI-EVA 的安装路径
3. **开始下载**: 点击"开始下载"按钮，启动器会自动：
   - 从 GitHub 下载 AI-EVA 项目
   - 克隆 IndexTTS2 仓库
4. **启动应用**: 下载完成后，点击"启动 AI-EVA"按钮

## 项目结构

```
AI-EVA-Launcher/
├── src-tauri/          # Rust 后端代码
│   ├── src/
│   │   └── main.rs     # 主程序入口
│   ├── Cargo.toml      # Rust 依赖配置
│   └── tauri.conf.json # Tauri 配置
├── index.html          # 前端界面
├── package.json        # Node.js 依赖配置
└── vite.config.js      # Vite 构建配置
```

## 技术栈

- **前端**: HTML + CSS + JavaScript (Vanilla)
- **后端**: Rust + Tauri
- **构建工具**: Vite
- **HTTP 客户端**: reqwest (Rust)
- **解压**: zip (Rust)

## 许可证

与 AI-EVA 项目保持一致。

