# AI-EVA Launcher 快速开始指南

## ✅ 图标文件已创建

占位符图标文件已成功创建在 `src-tauri/icons/` 目录：
- ✅ `32x32.png` - 32x32 像素图标
- ✅ `128x128.png` - 128x128 像素图标
- ✅ `128x128@2x.png` - 256x256 像素图标
- ✅ `icon.ico` - Windows 图标文件

## 🚀 下一步操作

### 1. 安装 Rust（如果未安装）

```bash
# 访问 https://www.rust-lang.org/tools/install
# 或运行：
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

Windows 用户可以直接下载安装器：https://forge.rust-lang.org/infra/channel-layout.html

### 2. 安装项目依赖

```bash
cd AI-EVA-Launcher
npm install
```

### 3. 开发模式运行

```bash
npm run tauri dev
```

这将：
- 启动 Vite 开发服务器（前端）
- 编译 Rust 后端
- 启动 Tauri 应用窗口

### 4. 构建生产版本

```bash
npm run tauri build
```

构建产物位于：
- Windows: `src-tauri/target/release/ai-eva-launcher.exe`
- macOS: `src-tauri/target/release/bundle/macos/ai-eva-launcher.app`
- Linux: `src-tauri/target/release/ai-eva-launcher`

## 📝 使用说明

1. **配置 GitHub 仓库**
   - 在界面中输入你的 AI-EVA GitHub 仓库地址
   - 例如: `https://github.com/your-username/AI-EVA`

2. **选择安装路径**
   - 默认路径: `~/AI-EVA` (用户主目录)
   - 可以修改为任意路径

3. **开始下载**
   - 点击"开始下载"按钮
   - 启动器会自动：
     - 从 GitHub 下载 AI-EVA 项目 ZIP 文件
     - 解压到指定目录
     - 克隆 IndexTTS2 仓库

4. **启动 AI-EVA**
   - 下载完成后，点击"启动 AI-EVA"按钮
   - 启动器会自动查找并执行启动脚本

## 🔧 故障排除

### 问题：Cargo 未找到

**解决方案**：
1. 安装 Rust: https://www.rust-lang.org/tools/install
2. 重启终端/命令行
3. 验证安装: `cargo --version`

### 问题：npm install 失败

**解决方案**：
1. 确保 Node.js 版本 >= 18: `node --version`
2. 清除缓存: `npm cache clean --force`
3. 删除 `node_modules` 重新安装

### 问题：Git clone 失败

**解决方案**：
1. 确保已安装 Git: `git --version`
2. 检查网络连接
3. 配置 Git 代理（如需要）

### 问题：图标文件缺失

**解决方案**：
```bash
cd src-tauri
python create_placeholder_icons.py
```

或运行：
```bash
create_icons.bat
```

## 📦 项目结构

```
AI-EVA-Launcher/
├── src-tauri/              # Rust 后端
│   ├── src/
│   │   └── main.rs        # 主程序逻辑
│   ├── icons/             # 应用图标
│   ├── Cargo.toml         # Rust 依赖
│   └── tauri.conf.json    # Tauri 配置
├── index.html             # 前端界面
├── package.json           # Node.js 依赖
└── vite.config.js         # Vite 配置
```

## 🎨 自定义图标

如果需要替换占位符图标：

1. 准备图标文件（PNG 格式，透明背景）
2. 使用在线工具转换：
   - ICO: https://www.icoconverter.com/
   - ICNS: https://cloudconvert.com/png-to-icns
3. 替换 `src-tauri/icons/` 目录中的文件

## 📚 更多信息

- Tauri 文档: https://tauri.app/
- Rust 文档: https://doc.rust-lang.org/
- 项目 README: `README.md`

## ⚠️ 注意事项

1. **首次编译较慢**: Rust 首次编译可能需要 5-10 分钟
2. **网络要求**: 下载和克隆需要稳定的网络连接
3. **磁盘空间**: 确保有足够的磁盘空间（至少 500MB）
4. **权限要求**: 某些操作可能需要管理员权限

## 🎉 完成！

现在你可以开始使用 AI-EVA Launcher 了！

如有问题，请查看日志输出或提交 Issue。

